// import _ from "lodash";

// const stylesMaping = {
//   visible: "display",
// };
// export const generateStyles = (model) => {
//   const stylesPick = ["Visible", "BackgroundColor", "Font", "TextColor"];
//   const result = _.pick(model, stylesPick);
//   return result;
// };
